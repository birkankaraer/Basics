@model List<Employee>

    <div class="display-3">
        Index 3
    </div>

@foreach (Employee emp in Model)
{
    <div class="lead">
        <p>@emp.FullName</p>
        <p>@emp.Id</p>
        <p><strong>Age:</strong>@emp.Age</p>
        <hr />
    </div>
}